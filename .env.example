# Gemini API key for AI-powered vote summaries
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Cloudflare Worker URL (optional - only needed for production)
# Local development automatically uses localhost:8787
# After deploying worker, update this with your worker URL
VITE_WORKER_URL=planning-poker-worker.your-subdomain.workers.dev
